// RangeSearch.h
#ifndef RANGESEARCH_H
#define RANGESEARCH_H

#include "kd_tree.h"					// kd-tree declarations
#include "kd_util.h"					// kd-tree utilities
#include "pr_queue_k.h"					// k-element priority queue

#include <ANN/ANNx.h>				   // all ANN includes

//#include "Polygon.h"
//#include <iostream>
//#include <vector>
/*
class RangeSearch {
private:
    ANNkd_tree* kdTree; // Pointer to a kd-tree instance
    
public:
// Constructor and destructor
    RangeSearch(const std::vector<Point>& points);
    ~RangeSearch();
    //Polygon Query relies on private variable kdTree, so object instance, can't be static
    int QueryPolyCount(const Polygon& P);
    std::vector<Point> QueryPoly(const Polygon& P);
    //void testRangeSearchFromFile(const std::string& pointFilePath, const std::string& queryFilePath);
};
*/


#endif
